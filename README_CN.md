# SQL 开发练习

这是 SQL 练习脚本集合，涵盖模式创建、数据完整性、索引、查询、嵌套查询和触发器。

> 课程说明：本目录收录课程实验或课程作业的解决方案。

## 脚本

- `Create.sql`：创建模式
- `Integrity.sql`：完整性约束
- `index.sql`：索引
- `select.sql` 与 `Nested_queries.sql`：查询
- `trigger.sql`：触发器

## 使用方式

请先在兼容的关系型数据库中执行 `Create.sql`，再按学习主题依次执行其他脚本。必要时请根据所用数据库产品调整语法。